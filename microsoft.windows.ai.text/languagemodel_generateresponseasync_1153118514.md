---
-api-id: M:Microsoft.Windows.AI.Text.LanguageModel.GenerateResponseAsync(Microsoft.Windows.AI.Text.LanguageModelContext,System.String,Microsoft.Windows.AI.Text.LanguageModelOptions)
-api-type: winrt method
---

# Microsoft.Windows.AI.Text.LanguageModel.GenerateResponseAsync(Microsoft.Windows.AI.Text.LanguageModelContext,System.String,Microsoft.Windows.AI.Text.LanguageModelOptions)

<!--
public Windows.Foundation.IAsyncOperationWithProgress<Microsoft.Windows.AI.Text.LanguageModelResponseResult,string> GenerateResponseAsync (Microsoft.Windows.AI.Text.LanguageModelContext context, string prompt, Microsoft.Windows.AI.Text.LanguageModelOptions options);
-->

## -description

Generates a response to a string prompt while returning progress for each token generated by the language model.

## -parameters

### -param context

The context window that defines the tokens a language model can process at one time for understanding an input prompt and then generating a response. The context window acts like the model's short-term memory, holding the input prompt and the preceding conversation to maintain coherence.

### -param prompt

The string prompt requiring a response.

### -param options

Options that affect how the language model responds to the prompt.

## -returns

The response to the prompt.

## -remarks

## -see-also

## -examples
